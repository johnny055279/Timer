<Window x:Class="TimerWpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TimerWpf"
        mc:Ignorable="d"
        Title="Timer" Height="520" Width="940" MinHeight="520" MinWidth="900" Background="#F6F2EA"
        PreviewKeyDown="Window_PreviewKeyDown"
        PreviewMouseDown="Window_PreviewMouseDown">
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#FFFFFF" />
            <Setter Property="Foreground" Value="#6C757D" />
            <Setter Property="BorderBrush" Value="#6C757D" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="10,6" />
            <Setter Property="MinHeight" Value="32" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              RecognizesAccessKey="True" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#6C757D" />
                                <Setter Property="Foreground" Value="#FFFFFF" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#5C636A" />
                                <Setter Property="BorderBrush" Value="#565E64" />
                                <Setter Property="Foreground" Value="#FFFFFF" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#E9ECEF" />
                                <Setter Property="BorderBrush" Value="#CED4DA" />
                                <Setter Property="Foreground" Value="#ADB5BD" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="BorderBrush" Value="#CED4DA" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="8,4" />
            <Setter Property="Background" Value="#FFFFFF" />
            <Setter Property="MinHeight" Value="28" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="Border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <ScrollViewer x:Name="PART_ContentHost"
                                          Margin="{TemplateBinding Padding}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="Border" Property="BorderBrush" Value="#86B7FE" />
                                <Setter TargetName="Border" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="#86B7FE"
                                                          BlurRadius="6"
                                                          ShadowDepth="0"
                                                          Opacity="0.6" />
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Border" Property="Background" Value="#E9ECEF" />
                                <Setter TargetName="Border" Property="BorderBrush" Value="#CED4DA" />
                                <Setter Property="Foreground" Value="#6C757D" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="BorderBrush" Value="#CED4DA" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="8,4,20,4" />
            <Setter Property="Background" Value="#FFFFFF" />
            <Setter Property="MinHeight" Value="28" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton x:Name="ToggleButton"
                                          Focusable="False"
                                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                          ClickMode="Press"
                                          Background="Transparent"
                                          BorderThickness="0">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border Background="Transparent">
                                            <ContentPresenter HorizontalAlignment="Stretch"
                                                              VerticalAlignment="Stretch" />
                                        </Border>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                                <Border x:Name="ComboBorder"
                                        Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        CornerRadius="4">
                                    <Grid>
                                        <ContentPresenter Margin="8,4,28,4"
                                                          VerticalAlignment="Center"
                                                          HorizontalAlignment="Left"
                                                          Content="{TemplateBinding SelectionBoxItem}"
                                                          ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                          ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}" />
                                        <Path Data="M 0 0 L 4 4 L 8 0 Z"
                                              Fill="#6C757D"
                                              HorizontalAlignment="Right"
                                              VerticalAlignment="Center"
                                              Margin="0,0,10,0" />
                                    </Grid>
                                </Border>
                            </ToggleButton>
                            <Popup x:Name="Popup"
                                   Placement="Bottom"
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   AllowsTransparency="True"
                                   Focusable="False"
                                   PopupAnimation="Slide">
                                <Grid MinWidth="{TemplateBinding ActualWidth}"
                                      MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                    <Border Background="#FFFFFF"
                                            BorderBrush="#CED4DA"
                                            BorderThickness="1"
                                            CornerRadius="4">
                                        <ScrollViewer SnapsToDevicePixels="True">
                                            <StackPanel IsItemsHost="True" />
                                        </ScrollViewer>
                                    </Border>
                                </Grid>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="ComboBorder" Property="BorderBrush" Value="#86B7FE" />
                                <Setter TargetName="ComboBorder" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="#86B7FE"
                                                          BlurRadius="6"
                                                          ShadowDepth="0"
                                                          Opacity="0.6" />
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="ComboBorder" Property="Background" Value="#E9ECEF" />
                                <Setter TargetName="ComboBorder" Property="BorderBrush" Value="#CED4DA" />
                                <Setter Property="Foreground" Value="#6C757D" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Grid x:Name="MainGrid"
          Margin="20"
          Background="Transparent">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Border x:Name="FocusSink"
                Width="0"
                Height="0"
                Focusable="True"
                Opacity="0"
                IsHitTestVisible="False" />

        <Border Grid.Column="0" CornerRadius="12" Background="#FFFDF8" Padding="16" Margin="0,0,12,0">
            <StackPanel>
                <TextBox x:Name="CountdownTitleTextBox"
                         Text="Countdown timer"
                         FontSize="18"
                         FontWeight="SemiBold"
                         BorderThickness="0"
                         Background="Transparent"
                         HorizontalAlignment="Center"
                         TextAlignment="Center" />
                <TextBlock x:Name="TimeDisplayTextBlock"
                           Text="00:00:00"
                           FontSize="36"
                           FontWeight="Bold"
                           Margin="0,12,0,12"
                           HorizontalAlignment="Center" />

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,10">
                    <Button Content="- mins" Width="80" Margin="0,0,6,0" Click="DecreaseTime_Click" />
                    <Button Content="+ mins" Width="80" Margin="0,0,6,0" Click="IncreaseTime_Click" />
                    <Button Content="Reset" Width="80" Margin="0,0,6,0" Click="ResetCountdown_Click" />
                    <Button x:Name="PauseButton" Content="Pause" Width="80" Click="Pause_Click" />
                </StackPanel>

                <StackPanel Margin="0,0,0,10">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Minutes step" VerticalAlignment="Center" />
                        <TextBox x:Name="StepMinutesTextBox"
                                 Text="5"
                                 Width="60"
                                 Margin="10,0,0,0"
                                 InputScope="Number"
                                 TextChanged="StepMinutesTextBox_TextChanged"
                                 PreviewTextInput="NumericOnly_PreviewTextInput"
                                 PreviewKeyDown="NumericOnly_PreviewKeyDown" />
                    </StackPanel>
                    <TextBlock x:Name="StepMinutesWarningTextBlock"
                               Text="Enter a number greater than 0."
                               Foreground="#DC3545"
                               FontSize="12"
                               Margin="0,4,0,0"
                               Visibility="Collapsed" />
                </StackPanel>

                <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                    <TextBlock Text="Select beep" VerticalAlignment="Center" />
                    <ComboBox x:Name="BeepComboBox"
                              Width="180"
                              Margin="10,0,0,0"
                              SelectionChanged="BeepComboBox_SelectionChanged"
                              DropDownClosed="BeepComboBox_DropDownClosed" />
                    <Button Content="Play" Width="60" Margin="10,0,0,0" Click="PlayBeep_Click" />
                    <Button Content="Browse" Width="70" Margin="10,0,0,0" Click="BrowseBeep_Click" />
                </StackPanel>

            </StackPanel>
        </Border>

        <Border Grid.Column="1" CornerRadius="12" Background="#FFFDF8" Padding="16">
            <StackPanel>
                <TextBox Text="Death counter"
                         FontSize="18"
                         FontWeight="SemiBold"
                         BorderThickness="0"
                         Background="Transparent"
                         HorizontalAlignment="Center"
                         TextAlignment="Center" />
                <TextBlock x:Name="DeathCountTextBlock"
                           Text="0"
                           FontSize="36"
                           FontWeight="Bold"
                           Margin="0,12,0,12"
                           HorizontalAlignment="Center" />
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <Button x:Name="IncreaseDeathButton" Content="+1" Width="80" Margin="0,0,6,0" Click="IncreaseDeath_Click" />
                    <Button x:Name="DecreaseDeathButton" Content="-1" Width="80" Margin="0,0,6,0" Click="DecreaseDeath_Click" />
                    <Button Content="Reset" Width="80" Click="ResetDeath_Click" />
                </StackPanel>

                <StackPanel Margin="0,12,0,0">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Step" VerticalAlignment="Center" />
                        <TextBox x:Name="DeathStepTextBox"
                                 Text="1"
                                 Width="60"
                                 Margin="10,0,0,0"
                                 InputScope="Number"
                                 TextChanged="DeathStepTextBox_TextChanged"
                                 PreviewTextInput="NumericOnly_PreviewTextInput"
                                 PreviewKeyDown="NumericOnly_PreviewKeyDown" />
                    </StackPanel>
                    <TextBlock x:Name="DeathStepWarningTextBlock"
                               Text="Enter a number greater than 0."
                               Foreground="#DC3545"
                               FontSize="12"
                               Margin="0,4,0,0"
                               Visibility="Collapsed" />
                </StackPanel>

                <TextBlock Text="Hotkeys (click and press keys)" Margin="0,12,0,6" />
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <TextBlock Text="Increase" Grid.Row="0" VerticalAlignment="Center" Margin="0,0,8,6" />
                    <TextBox x:Name="IncreaseHotkeyTextBox"
                             Grid.Row="0"
                             Grid.Column="1"
                             IsReadOnly="True"
                             Tag="Increase"
                             Margin="0,0,0,6"
                             PreviewKeyDown="HotkeyTextBox_PreviewKeyDown" />

                    <TextBlock Text="Decrease" Grid.Row="1" VerticalAlignment="Center" Margin="0,0,8,6" />
                    <TextBox x:Name="DecreaseHotkeyTextBox"
                             Grid.Row="1"
                             Grid.Column="1"
                             IsReadOnly="True"
                             Tag="Decrease"
                             Margin="0,0,0,6"
                             PreviewKeyDown="HotkeyTextBox_PreviewKeyDown" />

                    <TextBlock Text="Reset" Grid.Row="2" VerticalAlignment="Center" Margin="0,0,8,0" />
                    <TextBox x:Name="ResetHotkeyTextBox"
                             Grid.Row="2"
                             Grid.Column="1"
                             IsReadOnly="True"
                             Tag="Reset"
                             PreviewKeyDown="HotkeyTextBox_PreviewKeyDown" />
                </Grid>
            </StackPanel>
        </Border>

    </Grid>
</Window>
